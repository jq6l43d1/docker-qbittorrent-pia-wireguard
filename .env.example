# Private Internet Access (PIA) VPN Credentials
# Get these from your PIA account at https://www.privateinternetaccess.com/

PIA_USER=your_pia_username
PIA_PASS=your_pia_password

# VPN Server Location
# Default: ireland
# To see all available locations, run:
# docker run --rm -e USER=your_user -e PASS=your_pass thrnz/docker-wireguard-pia
# Common options: ireland, uk_london, us_california, us_east, netherlands, germany, etc.
PIA_LOCATION=ireland

# Instance Name (used for container naming to support multiple instances)
# Change this when running multiple instances on the same system
# Example: ireland, switzerland, us_east, etc.
INSTANCE_NAME=ireland

# qBittorrent WebUI Configuration
# Port for accessing the WebUI (change when running multiple instances)
WEBUI_PORT=8080

# User/Group Configuration
# Get your IDs with: id $(whoami)
# Important: Must match your user for proper file permissions
PUID=1000
PGID=1000

# Timezone
# Find your timezone: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# Examples: America/New_York, Europe/London, Asia/Tokyo, etc.
TZ=Etc/UTC

# Health Checks
# Both containers have automatic health monitoring:
# - pia-wireguard: Pings through VPN every 1 minute to verify connectivity
# - qbittorrent: Checks WebUI every 30 seconds to verify it's responding
#
# View health status with: docker compose ps
# Containers automatically restart if health checks fail 3 times in a row
# Start period allows time for initialization before health checks begin
